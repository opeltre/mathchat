html
  head
    link(rel='stylesheet', href='/views/style/main.css')
    title cloud
  body
    
    h1 cloud 
     
    div#files
      each f in files
        button mv
        button del
        a(href=`/files/${f.href}`, download=f.name)= f.name
        br

    h1 upload
    a(href='/upload') upload files
